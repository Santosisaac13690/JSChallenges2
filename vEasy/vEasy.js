// VERY EASY: Write a function named min that takes two arguments and returns their minimum.

function min(x,y) {
    return Math.min(x,y);
}
  
console.log(min(7,5), min(1,7), min(1,1), min(173,192));